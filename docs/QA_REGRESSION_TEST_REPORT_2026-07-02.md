# 视觉业务合理性回归报告

## 回归信息

- 日期：2026-07-02
- 基线：`QA_REGRESSION_TEST_REPORT_2026-07-01.md`
- 模块：多模态视觉判断、业务异常状态机

## 回归结论

“未观察到员工给顾客倒水”不再默认显示为正常。系统先识别服务对象是否在场，再判断必须服务行为是否发生；三态规则与原有垃圾类禁止目标规则均通过自动化回归。

倒水场景同时识别直接动作与服务结果。顾客手中、座位旁或其桌面的水杯/饮品可作为 `SERVICE_OUTCOME`，员工区或陈列区杯子不会被计为服务完成。

## 缺陷复测

| 编号 | 优先级 | 缺陷 | 修复结果 |
|---|---|---|---|
| VISUAL-POLICY-02 | P0 | 倒水等必须服务行为被默认归类为禁止目标，未观察到时错误显示“未发现异常” | 已修复，VLM 输出业务规则和服务对象状态，Agent 执行确定性三态判定 |
| UI-IMAGE-01 | P1 | 对话快照无法放大查看细节 | 已修复，支持点击/键盘打开大图、0.5-3 倍缩放、复位、遮罩与 Esc 关闭 |

## 判定矩阵

| 服务对象 | 必须行为 | 结果 |
|---|---|---|
| 在场 | 未发生 | `POSITIVE`，发现异常 |
| 不在场 | 未发生 | `NEGATIVE/NOT_APPLICABLE`，不触发异常 |
| 无法确认 | 未发生 | `UNCERTAIN/UNKNOWN`，需要复核 |
| 任意 | 已发生 | `NEGATIVE`，未发现异常 |

## 自动化记录

- `python3 online_agent_test.py`：通过。
- `python3 smoke_test.py`：通过。
- 需求文档结构校验：通过。
- 浏览器回归：桌面与 390×844 移动视口均通过；缩放、复位、Esc 关闭和焦点返回正常，页面无脚本错误。

## 周期快照巡检增量

已实现 `CREATE_SCHEDULED_INSPECTION` 对话链路、每日窗口补槽、一次确认、任务持久化、后台调度、快照归档、VLM 分析、运行结果回写和任务控制。模型分析使用归档后的图片字节，页面展示同一归档证据并携带 SHA-256，实现所见即所得。

自动化回归覆盖：确认前无副作用、计划状态回写、180 分钟间隔、营业时间窗口、门店在线镜头解析、任务创建、列表与详情、暂停、恢复、取消、取消后禁止恢复以及订阅视图展示。`python3 smoke_test.py` 已通过。

线上验收已完成：广州天河区天河城店 7 路摄像头全部抓图成功，首轮从触发到完成约 42 秒；Qwen3-VL 返回 `NEGATIVE`、可信度 99%，结论为地面未发现垃圾或杂物。随机抽取一张页面证据重新下载后，SHA-256 与运行记录完全一致。浏览器已验证 7 张快照、大图预览、已完成计划状态和订阅任务控制，控制台无错误。验收任务执行 1 次后已暂停，不会继续自动运行。

## AI 巡检历史增量

周期巡检结果已从聊天展示升级为独立历史记录。“告警与证据”新增 AI 巡检记录分段视图，按照 `run_id` 分页，一次执行只生成一条记录，其 7 路镜头图片作为同一记录下的 7 份证据展示。记录包含正常、异常、待复核、部分成功和失败状态。

自动化已验证：关闭原聊天后记录仍可查询；1 个批次和 7 条证据返回的列表总数为 1、详情证据数为 7；其他用户访问详情返回 404；非法页大小返回 400。`python3 smoke_test.py` 已通过。

真实浏览器回归读取到 2 个历史巡检批次，列表显示 2 条记录且每条标记 7 张证据；打开最新记录后详情实际渲染 7 张图片。390x844 视口无横向溢出，页面控制台无错误。

## 异常证据与消息时间增量

新增 `inspection_runs.anomaly_evidence_ids`，VLM 先保留每路候选镜头的业务状态，再将异常镜头映射到归档证据。对话结果卡、AI 巡检列表和详情共用同一异常证据字段。列表组合预览优先展示异常图，但不改变详情中的全部 7 张证据。

聊天消息展示服务端 `created_at`，统一转为 `Asia/Shanghai` 并精确到秒；周期巡检异步回复在模型分析完成时更新消息时间。

回归结果：`python3 online_agent_test.py` 与 `python3 smoke_test.py` 均通过。真实页面中 8 条聊天消息全部显示系统时间；17:16 异常批次共 7 张证据，仅“展厅5”1 张显示红框和“异常证据”，对话、列表、详情三处一致，控制台无警告或错误。

## 租户接入管理增量

新增“接入管理”页、`GET/POST /api/integrations`、`CONFIGURE_TENANT_INTEGRATION` 意图和 Agent 安全配置卡。环境变量接入作为只读托管项展示；对话新增接入使用 Fernet 加密保存，主密钥文件为 `0600`。

自动化结果：`python3 credential_vault_test.py`、`python3 online_agent_test.py` 和 `python3 smoke_test.py` 全部通过。冒烟用例验证非管理员 403、原始 AppKey/AppSecret 不进入历史消息、API 不返回密文、无效字段不触发外部连接。

真实浏览器回归：OPPO 租户展示脱敏 AppKey `jn9W********NX9g`，同步到广州天河区天河城店和一加旗舰店 2 家门店、合计 30 路摄像头。对话提交接入意图后出现完整安全配置卡，AppSecret 为 password 输入且 `autocomplete=new-password`。页面无横向溢出，控制台无警告或错误。

剩余风险：本版不在全局运行时直接切换新接入租户，以避免影响其他对话；多租户会话级数据源选择列为 P1。
