# Agent 日常操作链路回归报告

## 回归信息

- 日期：2026-07-01
- 地址：`http://127.0.0.1:8000/`
- 环境：OPPO DeepVision Online + Qwen3-VL-8B-Instruct-FP8
- 基线：`QA_AGENT_SKILL_REGRESSION_REPORT_2026-06-30.md`

## 回归结论

日常操作意图矩阵共执行 14 项，14 项通过。结构化意图已由 Qwen3 执行，确定性护栏覆盖媒体类型、历史时间、视觉判断和写操作状态。直播、回放、当前画面、历史预警、应用、设备状态、已有能力槽位和新能力 Pipeline 均命中预期 Skill。

## 缺陷复测

| 编号 | 优先级 | 缺陷 | 修复结果 |
|---|---|---|---|
| INT-ROUTE-01 | P1 | “给我看下门口的监控视频”返回 HELP | 已修复，命中 `VIEW_LIVE_STREAM` 并返回 HLS |
| INT-ROUTE-02 | P1 | “摄像头画面”返回设备清单 | 已修复，命中 `CAPTURE_SNAPSHOT` |
| INT-ROUTE-03 | P1 | 单纯查看画面触发异常分析 | 已修复，只有明确判断目标才进入 `ANALYZE_VISUAL` |
| MEDIA-SLOT-01 | P1 | 模型返回不完整回放时间对象后重复追问 | 已修复，本地解析补齐 `start_ms/end_ms` |
| PLAN-CONTEXT-01 | P1 | 第二轮补槽丢失已有能力并转为新 Pipeline | 已修复，继承能力、镜头和原始目标 |
| CONV-HISTORY-01 | P1 | 新建对话后旧消息从界面消失且无法恢复 | 已修复，会话列表、详情恢复和自动标题已接入 |
| CONV-HISTORY-02 | P1 | 历史列表截断为 15 条且无法手动关闭 | 已修复，完整展示最近 50 条，支持滚动、收起和单条软关闭 |
| MEDIA-PLAY-01 | P0 | 直播卡显示“播放中”但视频停在 0:00 | 已修复，避开超时 HLS，使用可达 HTTP-FLV，并以真实首帧/错误事件更新状态 |
| MEDIA-PLAY-02 | P0 | 浏览器直连供应商 FLV 后仍无首帧 | 已修复，新增带临时令牌的同源 FLV 分块代理，并禁用不稳定 Worker 路径 |
| INT-CONTEXT-01 | P0 | “视频里有没有员工在接待顾客”被误执行为新直播 | 已修复，意图改为 `ANALYZE_VISUAL`，继承上一直播镜头并抓取最新帧分析 |
| VISUAL-POLARITY-01 | P0 | “未发现员工接待顾客”错误显示为“未发现异常” | 已修复，必须行为缺失判定为异常；禁止目标缺失仍判定为正常 |

## 功能级结果

| 场景 | 输入 | 结果 |
|---|---|---|
| 历史预警 | 查看昨天的离岗预警 | `QUERY_ALARMS`，真实返回 3 条 |
| 实时直播 | 给我看下门口的监控视频 | `VIEW_LIVE_STREAM`，自动匹配展厅6并返回 HLS |
| 录像回放 | 查看昨天10点到10点10分门口录像 | `VIEW_PLAYBACK`，自动匹配展厅6并创建回放 |
| 历史图像 | 获取昨天10点05分门口监控画面图像 | `CAPTURE_SNAPSHOT`，明确等待历史抽帧工具 |
| 已上线应用 | 查看当前门店已经上线订阅了哪些应用 | `QUERY_SUBSCRIPTIONS`，返回 8 项 |
| 设备状态 | 查看服务器和摄像头在线离线状态 | 7 路在线、0 路离线；服务器接口缺失 |
| 已有能力首轮 | 给展厅1上线离岗巡检 | `NEED_CLARIFICATION`，缺日期、阈值、ROI |
| 已有能力补槽 | 补日期、推荐阈值、全画面 | 继承离岗和展厅1，进入 `NEED_INTEGRATION` |
| 非已有能力 | 上线顾客进店后3分钟内无人接待识别 | 生成 8 节点 `DRAFT` Pipeline |
| 历史对话 | 有消息后点击新建，再打开旧会话 | 恢复消息、计划和脱敏结果卡 |

## 自动化记录

- `python3 online_agent_test.py`：通过。
- `python3 smoke_test.py`：通过。
- `python3 -m py_compile agent_skills.py online_agent.py server.py online_agent_test.py`：通过。
- Qwen3 意图引擎：`llm`。
- Agent Skill：14 项。

## 剩余风险

1. 线上订阅创建接口契约仍缺失，槽位完整后只能停在 `NEED_INTEGRATION`。
2. 历史时刻图片需要录像解码与抽帧服务，当前不得以当前快照替代。
3. 边缘服务器健康接口缺失，只能返回摄像头在线状态。
4. 语义位置与镜头名称均不明确时，系统展示最高相关候选并标明低置信，不声称精确定位。

## 发布建议

可发布为日常查询、媒体查看、能力识别和编排的在线验收版本。订阅写入、历史抽帧和服务器健康继续保持受控阻塞。
