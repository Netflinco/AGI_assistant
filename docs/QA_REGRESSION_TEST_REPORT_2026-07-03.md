# QA 回归测试报告（2026-07-03）

## 范围

本轮验证租户接入意图优先级、完整凭证直连、失败无副作用、对话幂等和凭证脱敏。

## 结果

| 项目 | 结果 | 说明 |
|---|---|---|
| 凭证束意图 | 通过 | 无需动词仍命中 `CONFIGURE_TENANT_INTEGRATION` |
| 连接租户语义 | 通过 | 返回安全配置卡，不生成巡检 Pipeline |
| 有效凭证直连 | 通过 | 实际验证、同步、加密入库和结果卡路径完整 |
| 无效凭证 | 通过 | 返回 `BLOCKED`，不保存凭证 |
| 聊天与审计脱敏 | 通过 | 原始 AppKey/AppSecret 不出现在消息、标题、审计和列表 API |
| 对话幂等 | 通过 | 已存在租户返回当前接入，不覆盖凭证 |
| 页面回归 | 通过 | 接入管理展示 2 个租户；新租户 45 家门店；完整凭证对话直接返回成功卡，无控制台错误 |
| 多租户上下文 | 通过 | 请求级租户路由、加密凭证 Agent 实例、会话/计划/历史二次隔离已通过单元与 smoke 回归 |
| 切换体验 | 通过 | OPPO 与 `lianhe_tech` 往返切换不跳页；新租户展示 45 家门店并恢复上次门店；顶部、侧栏、欢迎语、快捷任务和任务详情文案随租户动态更新 |
| 真实新租户查询 | 通过 | `lianhe_tech` 中大银泰调用 `paas.camera.page` 返回 32 路摄像头；切换 OPPO 后再切回，门店和对话结果均正确恢复 |

## 自动化

- `python3 credential_vault_test.py`
- `python3 online_agent_test.py`
- `python3 smoke_test.py`

## 结论

本轮缺陷已闭环，租户接入已从通用 Agent 意图中独立为高优先级安全执行链路。
