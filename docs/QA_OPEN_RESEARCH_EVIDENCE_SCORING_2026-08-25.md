# 开放检索内容优先证据评分 QA（2026-08-25）

## 结论

通过。域名登记已从结果准入门槛改为信誉加权；安全、相关、时效和语义仍是不可绕过的证据边界。模型综合覆盖事件日期、实时状态、价格/航班、政策任命和稳定背景事实。

## 执行范围

执行命令：`python3 open_research_evidence_scoring_test.py`。

| 场景 | 预期 | 结果 |
| --- | --- | --- |
| 未加《》的影片“龙餐馆什么时间上映” | 识别事件日期；维基和百度百科交叉支持后可确定 | 通过 |
| 单条未登记上映来源 | 保留引用并降级为部分核验，不返回“未检索” | 通过 |
| 两个独立上映日期冲突 | 返回冲突，不渲染候选为结论 | 通过 |
| 新鲜天气 | 未登记来源可形成部分、带引用的实时答案 | 通过 |
| 过期天气 | 在模型前因时效不足拒绝 | 通过 |
| 实时股价 | 通用模型综合，不走影片专用逻辑 | 通过 |
| 航班当前状态 | 以实时状态意图和时效窗口交付 | 通过 |
| 商场是否营业 | 清理问句助词后正确计算相关性 | 通过；修复“是否”导致的低相关性 |
| 现任市长 | 政策/任命时效窗和结构化 Claim | 通过 |
| 企业成立时间 | 稳定事实经通用模型综合 | 通过 |

额外执行提示注入结果卡场景：不安全内容在证据标准化阶段丢弃，未进入模型包。

## 回归

已通过 `open_research_fact_gate_test.py`、`open_research_event_date_semantics_test.py`、`open_research_llm_evidence_reasoner_test.py`、`open_research_p05_test.py` 与 `open_research_test.py`。`open_research_office_smoke_test.py` 也已通过：会话、记录、ACL、来源点击、重新检索、Research→Office 工作流均完成本地 HTTP 验证。该集成夹具改为按查询返回对应证据，防止以“影片上映”页面错误支撑“最新政策”。前端静态语法检查使用随工作区提供的 Node 运行时执行。

## 遗留风险

来源信誉权重目前由平台配置且需要系统管理员异人复核；生产环境应持续根据用户“来源分级有误”反馈、来源稳定性和错误率调整权重。模型不可用时仍回退到确定性抽取，复杂通用事实会更保守地降级。
