# QA 回归测试报告（2026-07-04）

## 范围

本轮验证大规模门店搜索选择、楼层点位解析、批量视觉分析、模型输入全量展示及异常图片持久化标记。

## 结果

| 项目 | 结果 | 说明 |
|---|---|---|
| 门店选择器 | 通过 | 原生长下拉替换为固定高度可搜索弹层，支持名称和组织编号 |
| B1 点位解析 | 通过 | B1/B01/B001/负一层归一，B002 不会误入 |
| 楼层零匹配保护 | 通过 | 返回 `FLOOR_CAMERA_NOT_FOUND`，不分析其他楼层 |
| 多批视觉分析 | 通过 | 单测以 3 张图片、每批 2 张验证分批汇总 |
| 所见即所得证据 | 通过 | `media_gallery` 与实际 VLM 输入数量和镜头集合一致 |
| 异常图片标记 | 通过 | 只标记 `anomaly_camera_names` 命中图片，并进入会话持久化白名单 |
| 既有回归 | 通过 | 在线 Agent、smoke、凭证加密与前端语法检查通过 |
| 真实门店搜索 | 通过 | `lianhe_tech` 共 45 家门店；弹层 408px、结果区 320px 独立滚动；搜索“绍兴”返回 1 家 |
| 真实 B1 巡检 | 通过 | 绍兴金帝银泰城识别 5 路 B001 点位，抓图、模型输入、页面画廊和分析计数均为 5 |
| 历史刷新恢复 | 通过 | 刷新后仍展示 5 张图片、5 路计数及完整点位推理，控制台错误为 0 |

## 自动化

- `python3 online_agent_test.py`
- `python3 smoke_test.py`
- `python3 credential_vault_test.py`
- `python3 -m py_compile online_agent.py server.py`
- `node --check static/app.js`

## 结论

本轮功能与真实租户浏览器验收通过。异常图片正向标记由自动化用例验证：仅 `anomaly_camera_names` 命中图片持久化为 `is_anomalous=true`；本次真实 B1 画面结论为未发现异常，因此页面没有误标红。
