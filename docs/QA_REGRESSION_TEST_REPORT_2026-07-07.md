# QA 回归测试报告（2026-07-07）

## 范围

本轮验证同一批次预警与巡检画面的大图连续浏览。

## 结果

| 项目 | 结果 | 说明 |
|---|---|---|
| 批次识别 | 通过 | 从图片所在证据网格收集同批次图片，不跨消息或记录 |
| 上一张/下一张 | 通过 | 真实 5 图批次由 1/5 切换到 2/5，图片与镜头标题同步更新 |
| 首尾边界 | 通过 | 首张禁用上一张，末张禁用下一张 |
| 键盘操作 | 通过 | 方向键右移到 3/5，序号与图片同步 |
| 缩放状态 | 通过 | 每次切换恢复 100%，缩放按钮继续可用 |
| 既有回归 | 通过 | JavaScript 语法、online agent 与 smoke 测试通过 |
| B1 门店槽位纠偏 | 通过 | 模型返回 `poi_names=[B1层]` 时改用当前门店，不再返回组织未找到 |
| 明确楼层上下文 | 通过 | 即使历史存在 B2 图片且文本含“画面中”，仍重新抓取 B1 最新快照 |
| 租户链路追踪 | 通过 | Agent 返回当前 `tenant_code`，工具轨迹保留 `camera.floor.resolve` |

## 结论

批次图片浏览及租户/门店/楼层范围纠偏通过，可以发布到本地验收环境。
